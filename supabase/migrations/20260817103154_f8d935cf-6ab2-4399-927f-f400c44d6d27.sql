DROP FUNCTION IF EXISTS onecite.get_public_report(text);
COMMENT ON TABLE onecite.reports IS 'Reports are readable only by brand members (RLS) or server-side service_role. Public sharing must go through a server function that validates the share token.';