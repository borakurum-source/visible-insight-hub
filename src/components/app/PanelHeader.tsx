// Ported from client/src/pages/panel-header.tsx, trimmed down to the parts
// that make sense without a live backend: page heading + optional
// contextual action slot. Data-driven bits (usage badge, run-all progress,
// add/delete brand) now live inline on each page using panel-mock data.
export { PanelPageHeading, type PanelPageMeta } from "./panel-page-heading";
